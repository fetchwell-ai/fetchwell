.DS_Store
.vscode/

# Claude Code config — personal dev hooks
.claude/
CLAUDE.md

# Internal docs — ADRs, design handoffs, research spikes
docs/

# Bundled API key — generated file has the actual key, script encodes it
scripts/

# E2E test config
playwright.config.ts

# E2E tests that require real portal credentials (keep local only)
tests/e2e/stanford-e2e.spec.ts
tests/e2e/ucsf-discovery.spec.ts
tests/e2e/integration.spec.ts

# Dependencies and build output
node_modules/
dist/
dist-electron/
dist-renderer/

# Extracted health records — sensitive personal data, never commit
output/
!output/.gitkeep

# Credentials
.env
providers.json

# Packaged health records — sensitive personal data, never commit
mychart-*.zip

# Beads / Dolt files
.beads/
.dolt/
*.db
.beads-credential-key
test-results/
playwright-report/

# Runtime directories
downloads/

# TypeScript build cache
*.tsbuildinfo

# Generated bundled API key (produced by scripts/encode-key.ts — never commit)
electron/bundled-key.generated.ts

# electron-builder output
release/
*.bak
CertificateSigningRequest.certSigningRequest

# Personal / scratch
scratch.md
brainstorm.md
PRD.md
AUDIT.md
blurb.md
