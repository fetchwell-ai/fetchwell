# Anthropic API key — used by Stagehand for AI-powered browser actions
ANTHROPIC_API_KEY=sk-ant-xxxxx

# Health portal configuration — see providers.json (copy providers.example.json to get started)
# Provider URLs and credentials are now defined in providers.json, not here.

